# Work log — September 17, 2026

This log records today's decisions, code changes, runs, checks and unresolved issues for the later report. Times, where given, use Asia/Shanghai (UTC+8).

**Latest status:** the post-run update below records completion and validation of the declared HVE batch, 18 combined layer figures and the updated progress report. Earlier in-progress entries are historical snapshots.

## Decisions and constraints

- Preserve historical results and feature files; save new computations in this update directory.
- Retain participant-level three-fold train–test CV and the current fixed tau/distance definitions. No nested CV decision has been received.
- Keep SI likelihood diagnostics separate from the primary SBI z/ceiling displays.
- Treat a fixed common random-effect structure as an explicit sensitivity analysis; preserve the registered fallback analysis. Do not remove random effects solely to make warnings disappear.
- No Git commit/push, external message, feature extraction or t-SNE rerun is authorized or performed in these work batches.

## Batch 1 — initial implementation

| Work | What changed or ran | Evidence and result |
| --- | --- | --- |
| Acoustic scaling | Added `acoustic_subset: global_z` to the main and four sensitivity profiles; recomputed 14 components from exact subsets of full-baseline coordinate moments | 5,459 pairs and 5,685 available responses per feature. Full MFCC/STRF distances reproduced within `1.78e-15`. See `acoustic_diagnostic/preparation_checks.json`. |
| Component models | Full MFCC39/STRF24 plus 14 components; all four models, full data and three training folds | 256 selected fits, no fit/singularity/convergence flags. Component ordering remains affected by random-structure fallback. See `acoustic_diagnostic/report_metadata.json`. |
| Matched scoring | Added separate training/test fixed-effect prediction scores and word-response counts to the R runner; included them in the parallel output merger | Nine runs, 46 feature specifications, 1,104 split-score rows, 736 selected fits without flags. New test scores equal existing held-out metrics. See `train_test/tables/summary_validation.json`. |
| Layer profiles | AN19 base/FT all 18 registered layers; X21/B23 Tr-24 and full acoustic baselines in all datasets | Two direct training/test loss plots; all scores retained. The ratio was not drawn before clarification. |
| Within-condition check | Refit full MFCC/STRF separately within six AN19 exposure/test conditions, preserving original folds | 96 selected fits; 20 boundary-singularity flags in two conditions. Both predictors improve pooled held-out prediction in the other four conditions. See `acoustic_diagnostic/within_condition/audit_metadata.json`. |
| Optimizer audit | Inspected historical Optuna/SciPy searches and current fixed-parameter code | Parameter ranges, penalties, seeds and target refits documented in [OPTIMIZER_AUDIT.md](../sbi/OPTIMIZER_AUDIT.md); no historical optimization rerun. |
| Numerical verification | Shared-standardizer checks, frozen-score R integration check, plot-summary checks and existing project checks | Focused R test passed; latest Python run passed 36 checks, with dependency/deprecation warnings. Three real-data contract checks also passed in the earlier full suite. An initial scientific-environment discovery lacked pytest; checks were rerun successfully in the existing test-capable environment without installing packages. |

Source changes from this batch: `R/fit_confirmatory.R`, `src/ctg/parallel_glmm.py`, five analysis profiles, three new analysis scripts, focused numerical checks, the scientific specification/runbook, and both TODO entry points. Existing unrelated working-tree changes were preserved. The [update README](README.md) links the reviewed outputs and reproduction commands.

## Batch 2 — diagnostic evaluation

| Work | Planned output | Status before the 15:32 handoff |
| --- | --- | --- |
| Approved SI ratio | `build_train_test_ratio_figures.py`, fold-wise tables, ratio=1 reference and three-fold intervals | Initial nine-run build passed: 552 paired ratios. The expanded SBI build has 1,368 ratios and six complete layer profiles, with zero invalid pairs or warnings. Four repeated Tr-24 runs reproduce all shared score/formula columns exactly (maximum numeric difference 0). HVE figures will be added after fitting. |
| Remaining SBI layer scores | X21/B23 base and FT at all 18 registered layers, preserving existing feature values/folds | All four runs completed in `tt_all/`: 72 specifications, including four Tr-24 specifications repeated for consistency checks. Final combined coverage must omit the older duplicates. |
| HVE diagnostic coverage | All available Tr-24 measures plus `overall` and `overall_order_sensitive` across the remaining 17 layers | Inventory and preparation complete; 78 + 204 specifications are being fitted with all four models. B23's 97- and 168-participant samples remain separate. No predictor retuning. |
| Common-structure sensitivity | Optional fixed participant/item random-intercept policy; variance tables and controlled acoustic/condition reruns | Completed: 352 selected fits, no failures, 20 boundary-only singular fits. All-condition feature rankings unchanged (rank correlation 1 for both mean training z and pooled test loss). Primary result hashes unchanged. |
| Source identity checks | Independently check actual response identities, values, missing statuses, folds and participant strata | 814 checks passed, no failures. All four expanded SBI Tr-24 inputs exactly match retained inputs; HVE Tr-24/global identities match in each stratum. See `source_validation/input_identity_validation.json`. |
| Calculation checks after integration | Complete Python suite and two R integration checks | 53 Python checks passed in 178.73 seconds; three real-data contract checks passed separately in 179.71 seconds. Both R checks passed: frozen split scoring and unchanged registered default/optional common policy. |

### Details retained for the later report

- The SI ratio pairs train/test within each fitted fold before taking the mean. Its interval enumerates all 27 ordered bootstrap resamples of the three paired ratios; it describes fold variability with overlapping training samples. All four models enter tables, while layer figures display `M_predictor`. Nonfinite/zero denominators and missing pairs are flagged; model warnings remain visible.
- HVE was inventoried from the revised all-layer inputs and the September 6 Tr-24 inputs. AN19 has seven existing measures, X21 has 17, and B23 has 15 across two sample strata. Only the two overall definitions are being extended across all layers in this batch; other definitions receive Tr-24 diagnostics. No behavior-based winner was selected for this coverage choice.
- `run_hve_train_test_diagnostics.py` prepares chunk-filtered inputs and checks identical response identities across features within each stratum. AN19: 120 participants/5,760 word responses; X21: 320/16,477; B23 non-global-order: 168/54,096; B23 global-order: 97/31,234. AN19's later acoustic phone illustrations do not imply that phone-level exposure HVE has been recomputed.
- The first HVE Tr-24 batch used runner SHA-256 `dc764a8d9401f5692ae0d7cd8579718c199e3b272df96252fc612879cac70501`. The subsequent preparation added `--exclude-tr24` to avoid duplicate global-profile fits; that revision's hash is stored in `hve_global/run_inputs.json`.
- `participant_item` is an optional AN19 sensitivity; the default remains `registered`. Cache keys now include the policy and require `variance_components.csv`. The default versus explicitly registered numerical check passed. Shared R source was frozen during these runs at SHA-256 `589076774f7c9184cb3801c8b4ef2d9d702f59f6f4d90ad8ad2cc39ecac9bcdf`.
- In the common-structure rerun, Korean-exposure/Spanish-test fold 0 and Spanish-exposure/Spanish-test full/all-fold fits have near-zero participant variance. Item variance remains. All 20 convergence messages are boundary-only; there are no additional convergence messages. Four clean conditions retain improvements for both baselines. See `common_structure/flagged_variance_components.csv` and `result_metadata.json`.
- Runs used bounded concurrent R workers, with numerical-library threads limited to one. Remaining SBI used two runs × ten workers; Tr-24 HVE used two × four; global HVE uses two × six after the SBI queue was nearly finished. Windows R launcher/worker pairs can produce two process entries per actual fit.
- One attempted inline Python launch failed on PowerShell quotation parsing before executing scientific code. The corrected stdin launch started from the existing prepared manifest, preserving inputs. No data were deleted or changed by that failure.
- The 53-check Python run reported existing optional-dependency version warnings for numexpr/bottleneck and a Jupyter path deprecation. R reported Windows locale warnings and a Matrix build-version warning. The synthetic common-policy check deliberately retains boundary fits. None of these caused a failed check; no dependency upgrades were performed.
- SBI visual inspection covered the X21/B23 profiles; the legend was moved above the plotting area to avoid overlap with intervals. The mean neural-layer ratios range from 0.999615 to 1.000893 in AN19, 1.005850 to 1.006788 in X21, and 1.000519 to 1.000690 in B23 across both variants. Individual fold variability is larger than the range of layer means. These diagnostics do not establish that selecting the best layer on the same CV scores is unbiased.

### Commands and run records

Run from the repository root with the existing analysis environment; no packages were installed.

```text
python cross_talker_generalization/scripts/run_an19_common_structure.py --jobs 4
python cross_talker_generalization/scripts/run_an19_common_structure.py --report-only
python cross_talker_generalization/scripts/run_hve_train_test_diagnostics.py --output cross_talker_generalization/analysis/hve/train_test --jobs 4
python cross_talker_generalization/scripts/run_hve_train_test_diagnostics.py --output cross_talker_generalization/analysis/hve/global_order --global-profiles --exclude-tr24 --prepare-only
python cross_talker_generalization/analysis/diagnostics/source_validation/verify_input_identity.py
Rscript cross_talker_generalization/tests/test_train_test_scores.R
Rscript cross_talker_generalization/tests/test_random_structure_policy.R
python -m pytest cross_talker_generalization/tests -q --ignore=cross_talker_generalization/tests/test_project_contract.py
```

The global HVE prepared manifest is executed through `fit_glmm_parallel` with `predictor_column="predictor_value"`, `direction=1`, `term="variability_z"`, `model_set="all"`, six workers per run and two concurrent runs. Remaining SBI uses the inputs in `tt_all/run_inputs.json` with the default distance predictor and `model_set="all"`. Both retain the registered random policy. The final nonduplicated source list is `diagnostic_inputs.json`; the ratio builder consumes that list. Common-structure exact commands, versions, input hashes and unchanged-primary checks are in `common_structure/run_record.json`, `report_record.json` and `result_metadata.json`.

## Outstanding scientific decisions

### Usage-limit handoff, 15:32

The user requested an immediate progress report and deferred later results. The [stage report](PROGRESS_REPORT.md) was created; no additional fits were launched. Existing HVE jobs remain active and save local outputs. Snapshot: 200 of 282 specifications have individual diagnostic files; 118 are in eight fully merged runs. The final combined manifest/18-profile package has **not** been assembled or validated. The report includes resume commands; later completion must be checked separately.

Additional checks completed before handoff: the [figure-input audit](source_validation/FIGURE_INPUT_AUDIT.md) establishes the 20 ms Tr-24 stride, 25 ms frontend receptive field and 138-word/42-talker matrix; no figures were changed. The [HVE repeat check](source_validation/hve_repeated_tr24_score_validation.json) compares six finished runs against September 6: five pass the strict numerical tolerance; X21-FT within-type-phoneme loss differs by up to `3.60e-7`, with unchanged response/fold/formula/fit-flag identities and predictor differences up to `5.68e-14`. The difference remains recorded. The two B23 168-participant runs were not yet compared. An independent review confirmed paired-fold ratios, CI arithmetic and B23-stratum separation. It identified a manifest-key mismatch (`input_sha256` versus `sha256`), which the prepared final assembler explicitly normalizes and verifies.

Eight active Markdown documents passed local-link checks; the figure audit's 12 source links also resolve. Ratio figures were visually inspected. No full HVE figure QA or final combined-results claim has been made.

Nested selection has not been implemented in these runs. The matched z-versus-likelihood parameter-search comparison, its search stability, full manuscript panel coverage and the interpretation of AN19's strong baseline results remain open. Boundary fits and any incomplete batches must remain visible in the later report.

## Post-run update — after 16:06

At the user's request, the finished local outputs were assembled, validated and plotted. No new model fitting, feature extraction or parameter search was started.

- Both HVE batches are complete: 78 Tr-24 and 204 additional-layer specifications, in 16 merged run directories. Combining them with 114 nonduplicated SBI specifications gives **25 runs and 396 specifications**.
- The final check passed: **9,504 split-score rows**, **4,752 paired-fold ratios**, complete four-model/three-fold/two-split grids, matching input/output hashes and agreement with fold-level and pooled CV losses. All scores have status `ok`; no invalid ratios or CV fit warnings are present in this batch. The run directories contain 6,336 selected full/training-fold fits. This does not remove the 20 boundary warnings in the separate AN19 common-structure sensitivity.
- Generated **18 layer profiles (6 SBI + 12 HVE)** in PNG/PDF/SVG. Other 66 HVE dataset/variant/method profiles remain Tr-24 table-only. B23's 97- and 168-participant samples remain separate. HVE representative PNGs for AN19 overall/base, X21 overall/base and B23 global-order/FT were visually checked: labels and intervals are visible without clipping.
- Rechecked all eight HVE Tr-24 runs against September 6: seven meet the strict tolerance. The previously recorded X21-FT within-type-phoneme difference remains; maximum mean-loss difference is `3.60e-7`, with unchanged response/fold/formula/fit-flag identities. This historical-repeatability check remains flagged, independently of the passing current-batch arithmetic checks.
- Updated `PROGRESS_REPORT.md`, the update README, the SI guide, root `TODO.md` and the Chinese meeting TODO. Only the completed diagnostic scope was marked finished. The z/likelihood optimization comparison, nested-CV decision, ceiling corrections and remaining manuscript panels stay open.

Commands executed successfully:

```text
python cross_talker_generalization/analysis/diagnostics/source_validation/assemble_diagnostic_inputs.py --assemble
python cross_talker_generalization/scripts/build_train_test_ratio_figures.py --manifest cross_talker_generalization/analysis/diagnostics/diagnostic_inputs.json --output cross_talker_generalization/analysis/diagnostics/si_diagnostics
python cross_talker_generalization/analysis/diagnostics/source_validation/assemble_diagnostic_inputs.py --validate
python cross_talker_generalization/analysis/diagnostics/source_validation/verify_hve_repeated_tr24_scores.py
```

Evidence: [final validation](source_validation/final_validation.json), [profile summaries](source_validation/profile_summary.csv), [figure inventory](si_diagnostics/tables/figure_inventory.csv), and [historical-repeatability audit](source_validation/hve_repeated_tr24_score_validation.json). The prior SBI-only manifest is retained as `diagnostic_inputs_sbi.json`.
