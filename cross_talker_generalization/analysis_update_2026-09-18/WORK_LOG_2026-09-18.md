# Work log — September 18, 2026

## Completed work

1. Recorded Zhengyang's explicit choice of signed z for the planned optimization comparison.
2. Updated `src/ctg/ceiling_cv.py`: AN19/X21 condition keys, X21 sentence context, fold/count validation, explicit unavailable-cell handling, scored-response denominators, and source/version records. B23 retains its prior cell definition.
3. Added six calculation checks in `tests/test_ceiling_cv.py`: separation of conditions, no held-out-response influence on its own prediction, X21 context separation, missing-cell behavior, grouped word-response denominators, and duplicate-fold rejection. All six passed.
4. Added and ran `scripts/audit_condition_ceiling.py` on all three datasets. Historical and new response identities and counts match exactly. Every response receives a prediction. Saved paired tables, source hashes and PNG/PDF/SVG comparisons in `conditional_ceiling/`.
5. Visually checked the comparison PNG: labels, paired fold lines and axes are readable without clipping.
6. Updated the English project TODO, Chinese meeting TODO and scientific specification. Separated the completed key correction from remaining sample matching and z-ceiling work.
7. Ran the complete Python test directory: **59 passed in 105.31 seconds**. Three existing dependency/deprecation warnings were reported; no packages were changed. `git diff --check` on the changed tracked source/specification/TODO files passed.

## Interpretation and boundaries

All three ceiling computations finished. AN19 and X21 losses changed; B23 reproduced to floating-point precision. These outputs use all behavioral test-phase rows, split by participant. They have not been substituted into existing theoretical-model normalization or z figures. The comparison does not establish which part of X21's change is due to condition versus sentence context.

At the earlier ceiling handoff no optimization experiment had run. Subsequently, Zhengyang supplied the Yuhao/Florian optimization email and authorized a cost-bounded pilot; that later work is recorded below. No nested CV, feature extraction, t-SNE reduction, package installation, Git commit/push, or collaborator message was performed. Historical files were preserved. Pre-existing staged and unstaged work was not reset or restaged.

## Later session: cost-bounded SBI optimizer pilot

1. Honored Zhengyang's decision to pause HVE and his instruction to defer expensive execution.
2. Added `R/fit_sbi_optimizer_probe.R` and `scripts/probe_sbi_joint_optimization.py`: joint tau/k candidate search, existing hard-DTW/mean-distance aggregation, fixed predictor-only binomial GLMM structure, signed-z and fitted-likelihood objectives, no penalty, no held-out scoring. Enforced a 600-second run budget, per-fit timeout, exact-candidate caching and four DTW threads.
3. Ran AN19 base 3-D t-SNE Tr-24, training folds 1+2 only (3,791 responses, 80 participants, 5,394 physical pairs). Nine initial parameter pairs were followed by twelve proposals per method/objective for grid, TPE and differential evolution, seed 42. All 39 distinct fits converged without singular estimates. Total elapsed time: 101.21 seconds.
4. Tau=2 distances reproduced within `1.42e-14`. Added and ran `scripts/summarize_sbi_optimizer_probe.py`; checked matching rows, exponential transformation/scaling, z arithmetic, search traces and full proposal coverage. Both new Python scripts passed compilation checks. These checks are additional to the earlier 59-test suite, which was run before these pilot scripts were added.
5. Generated and visually checked two PNGs: nine-point parameter probe and optimizer progress. Saved settings, code/input hashes, fit logs, timings, detailed scores and the [pilot report](sbi_optimizer_probe/README.md).
6. All methods selected the same pair under their two respective objectives in this tiny run. The grid's best pair was tau=1, k=0.001, on both lower bounds. No global-optimum or optimizer-superiority conclusion was made. Shared caches invalidate direct algorithm-speed ranking from recorded elapsed times.
7. Deferred the full benchmark: a 720-proposal example would take roughly 30 minutes for this one layer/split at measured serial rates. No additional computations remain running from this pilot.

## Dense parameter landscape (subsequent request)

Zhengyang subsequently authorized a larger parameter map, then explicitly prioritized X21. The initial AN19 grid was interrupted; partial results and pre-change sources were retained under `sbi_parameter_landscape/` and marked not for analysis. The all-range expm1 formulation in that initial batch was also unsuitable for large k; it was replaced by a stable shifted exponential with an expm1 branch restricted to small exponent spans.

The replacement X21 run is in `x21_parameter_landscape/`: 1,247 tau/k pairs and 29 small-k limit fits, Tr-24 base t-SNE, training folds 1+2 only. It uses the registered X21 talker fixed-block/participant-item random-intercept structure and eight R workers. A 30-minute budget was imposed. Twelve numerical checks of the actual R scaling expressions passed, and tau=2 distances were checked against the stored input.

Completion: all 1,276 fits converged without singular estimates in 949.89 seconds. Coverage, row identity, held-out exclusion, source hashes, standardized-predictor moments and z arithmetic passed. Added `scripts/plot_sbi_landscape.py`, saved seven PNG/SVG figure pairs and the summary tables, and visually checked the 3-D surfaces, heatmaps and profiles. Adjusted 3-D layout to keep axis labels readable. Added `R/check_sbi_landscape_peaks.R`; independent Nelder-Mead refits of both domain winners differed by less than 4.5e-6 in fitted likelihood and 0.00072 in z. These checks are additional to the earlier 59 Python tests; the complete test suite was not rerun after adding the landscape scripts.

The [completed analysis](x21_parameter_landscape/README.md) reports identical best-z and best-likelihood grid coordinates, separates tau<1 sensitivity from the metric domain, and documents the tau boundary and tau/k ridge. One split/layer does not settle optimization-method superiority or held-out prediction. Updated both TODOs and the progress report. No landscape computation remains running; HVE remains paused.

At Zhengyang's request, added a self-contained Plotly HTML export (about 4.9 MB), with synchronized rotation, zoom, exact-grid-point hover readouts and PNG export. Browser inspection through a temporary loopback preview confirmed surface rendering, drag rotation and synchronized views. Surface-interpolated hover was disabled; numerical readouts use actual fitted scatter points. No GLMMs were rerun for this interactive export.

## Talker-specific exclusion sensitivity

Zhengyang then requested a version excluding Talker-specific. Added `run_sbi_landscape_subset.py`, reusing each cached tau distance table and removing 2,679 responses before standardization/fitting. Retained 8,290 responses / 161 participants in Control, Single-talker and Multi-talker; original folds and model structure were unchanged. Removed the inherited stale `similarity_exp_k` column from these new cached inputs. The original run and source code hashes remain intact.

All 1,276 fits completed without convergence or singular-fit problems in 378.35 seconds, using 16 R workers. Validation passed; generated seven PNG/SVG pairs and a standalone interactive map. Added `summarize_sbi_subset_predictors.py` to export two distinct winning candidates' actual raw distances, exp(-k*d), and re-standardized predictor, plus condition distributions and a clearly labeled cross-sample parameter comparison. Both new scripts passed Python compilation. No entire-suite rerun was performed.

The metric-domain winner is tau=1, k=0.01791411, signed training z=4.092719, fitted log likelihood=-3162.096992. The expanded-domain winner is tau=0.5, k=0.00603418, z=4.147746, log likelihood=-3161.879799. Both objectives agree on these points. Two independent inner-solver checks agreed within 0.000011 z and 0.000008 log-likelihood units. Existing R locale/Matrix-version startup warnings remain. No held-out scoring, new HVE computation, feature extraction, Git operation, or external sharing was performed.

Visual QA confirmed the new static 3-D/2-D figures and the exclusion label on the interactive figure. A temporary loopback browser preview verified synchronized rotation and exact fitted-point hover readouts. The preview was closed after inspection. All statistical fits are complete; source-cache files and the original result directory were preserved.

## Presentation and pause

Created a two-slide English PPTX with the two existing static previews and relative hyperlinks from both images and link labels to the matching offline HTML files. Packaged copies beside the PPTX, with usage instructions. Rendered both final slides for visual review and checked both hyperlink relationships against their actual targets. The deck was not tested in native PowerPoint; local-link restrictions may require opening the HTML directly. No numerical outputs changed.

Recorded Zhengyang's pause of further parameter-search research. Reviewed the saved September 11 meeting requirements, September 15 email and existing progress/TODO records, and wrote `NEXT_FLORIAN_TASKS.md`. It separates explicit unfinished tasks from conditional proposals, completed diagnostics and decisions awaiting a reply. Recommended resumption outside optimization: explain the AN19 high result. No collaborator messages, new model runs or Git commits were made.
